**Extract the following information from the provided text and format it as a JSON object with concise, summarized data:**

{{ fields_info }}

**Instructions:**

0. Remove 'type' and 'description' fields for all returned data.
1. Ensure all required fields are filled.
2. Omit optional fields if the information is not present in the text.
3. Format date fields as `YYYY-MM-DD`. If month and/or day are missing, assume `01` for both. Ignore present dates.
4. Format boolean fields as `true` or `false`.
5. Format list fields as arrays of items.

**Text to extract from:**
{{ text }}

**Respond only with the JSON object.**