# parse through resumes to build a model
# this will use both the pdf reader and the llm

# 1. get a resume
# 2. extract data and metadata
# 3. ask the llm to parse through the data

# output/return a resume model (for now preferrably to txt in a folder)
