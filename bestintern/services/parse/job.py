# parse through job links to gather data and build job models
# uses both the web reader and the llm

# 1. get a link
# 2. determine a way to get the data
# 3. ask the llm to parse through the data

# output/return a job model (for now preferrably to txt in a folder)
